Add this to ox_inventory/data/items.lua

	["phonetracker"] = {
		label = "Tracker",
		weight = 200,
		stack = false,
		close = true,
	},

Also add image from the folder to ox_inventory/web/images